# Secrets & local config — never commit
.env
settings.json
installation_id

# Runtime data (generated by Qwen Code when this repo is used as ~/.qwen)
telemetry/
tmp/
data/
projects/
sessions/
memories/
todos/
usage/
file-history/
debug/
*.jsonl
tip_history.json
usage_record.jsonl

# Evolution pipeline outputs (scripts are tracked, generated data is not)
evolution/*.json
evolution/*.jsonl
evolution/*.md

# Dependencies & build artifacts
node_modules/
__pycache__/
*.pyc
*.db

# OS/editor noise
.DS_Store
Thumbs.db
