---
description: "Trigger full-stack orchestration (DB -> Backend -> Frontend -> Animation)"
---
Delegate this task to the fullstack-orchestrator agent. Ensure database-architect runs first, then backend, then frontend, then animation.
